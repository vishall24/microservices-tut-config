spring:
  application:
    name: config-prod

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defafaultZone: http://localhost:8761/eureka

